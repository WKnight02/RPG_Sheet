<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.uqac.android.projet.rpgsheet" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".View.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".View.CharactersView"></activity>

        <activity android:name=".View.CharacterView"></activity>

        <activity android:name=".View.StoriesView"></activity>

        <activity android:name=".View.StoryView"></activity>
        
        <activity android:name=".View.MonsterView"></activity>

    </application>
</manifest>
